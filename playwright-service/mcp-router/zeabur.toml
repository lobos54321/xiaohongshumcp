# Zeabur MCP Router 部署配置

[source]
root = "."

[build]
command = "npm run build && npm run start:http"

[deploy]
port = 3000
health_check = "/health"

[runtime]
version = "20"

[env]
# 这些环境变量可以在Zeabur控制台中调整
# BASE_PORT
# HTTP_PORT
# MAX_PROCESSES
# CLEANUP_TIMEOUT